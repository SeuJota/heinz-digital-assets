.col-sm-2.heinz.text-center
  %img.overfquero.ovlay(src="/assets/overflow.png")
  =link_to image_tag("heinz_header.png", style: "z-index: 2;width: 100%;", class: "centered"), category_path(id: "heinz"), :class => "heinzsmall"
.col-sm-10.querohd.text-center{style: "padding-top: 3%; min-height: 100%;"}
  .nothing
    = image_tag("quero_header.png")
    %h1{style: "color: white; margin-bottom: 15px; margin-top:25px; font-size: 2em;"}
      - @category.ancestors.each do |cat|
        = link_to cat.name, cat, style: "color: white"
        = image_tag("arrow.png")
        - if cat == @category.ancestors.last
          = @image.name
    .col-sm-5{style: "text-align: right;"}
      = image_tag(@image.avatar.url(:show), style: "margin-top: 50px;margin-bottom: 20px;")
    .col-sm-6{style: "color: rgba(210, 210, 210, 0.960784); font-size: 15px;"}
      = render "images/public/info"
      / %p{style: "font-size: 18px; color: white; margin-left: 20px;"}
      /   %b
      /     \-
      /     = @image.name
      / %p
      /   Categoria:
      /   %span{style: "color: white;"}=@image.category.name
      / %p
      /   Código de produto:
      /   %span{style: "color: white;"}=@image.code
      / = render "images/public/table"
      = render "images/public/table"

    / .col-sm-12{style: "color: rgba(210, 210, 210, 0.960784); font-size: 15px; text-align: left;"}
    /   = render "images/public/table"
