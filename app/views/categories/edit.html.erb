<div class="jumbotron" style="padding: 20px;">
	<div class="container">
		<h1>Editando <%= @category.name %><br/>
		<small>Superior: <%= @category.parent ? @category.parent.name : "root" %></small></h1>
	</div>
</div>
<div class="container">
	<%= render 'form' %>
</div>
