.jumbotron{style: "padding: 20px;"}
	.container
		%ul.list-inline
			%li
				%h1 
					#{@category.name}
					%small Imagens
			%li= link_to 'Nova Imagem', new_category_image_path(category_id: @category.slug), method: :get, :class => "btn btn-info btn-sm", style: "margin-bottom: 20px;"
			%li= link_to 'Voltar', category_path(@category), :class => "btn btn-warning btn-sm", style: "margin-bottom: 20px;"
.container
	%table{class: "table table-striped table-bordered table-condensed"}
		%thread
			%tr
				%th.text-center #
				%th.text-center Name
				%th.text-center Image
				%th.text-center Actions 
		- @images.each do |img|
			%tr
				%td{:style=>"vertical-align: middle;", :class=>"text-center"}= img.id 
				%td{:style=>"vertical-align: middle;", :class=>"text-center"}= img.name
				%td{:style=>"vertical-align: middle;", :class=>"text-center"}= link_to image_tag(img.avatar.url(:thumb)), category_image_path(@category.id, img.id)
				%td{:style=>"vertical-align: middle;", :class=>"text-center"}
					= link_to "Editar", edit_category_image_path(id: img.id), :class => "btn btn-primary btn-sm edit"
					= link_to "Destruir", category_image_path(@category.id, img.id), method: :delete, data: { confirm: 'Tem certeza?' }, :class => "btn btn-danger btn-sm"
