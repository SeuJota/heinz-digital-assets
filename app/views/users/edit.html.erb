<div class="jumbotron">
	<div class="container">
		<h2>Editando <%= @user.name %> <small>ID do usuário:<%= @user.id %></small></h1>
	</div>
</div>
<div class="container">
	<%= render 'form' %>
	<div class="text-center">
		<%= button_to "Destruir", category_path(id: @user.id), method: :delete, data: { confirm: 'Tem certeza?' }, :class => "btn btn-danger" %>
	</div>
</div>
