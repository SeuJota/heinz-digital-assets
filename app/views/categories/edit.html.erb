<div class="jumbotron" style="padding: 20px;"> 
	<div class="container"> 
		<h1>Editing <%= @category.name %><br/> 
		<small>Parent: <%= @category.parent ? @category.parent.name : "root" %></small></h1>
	</div>
</div>
<div class="container">
	<%= render 'form' %>
</div>
